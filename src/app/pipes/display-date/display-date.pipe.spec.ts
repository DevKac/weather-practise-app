import { DisplayDatePipe } from './display-date.pipe';

describe('DisplayDatePipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayDatePipe('en-US');
    expect(pipe).toBeTruthy();
  });

    // @TODO(anbelz): testy do dopisania
});
